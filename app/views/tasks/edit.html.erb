<% if logged_in? %>

<h1>id:
<%= @task.id %>
のタスク編集ページ</h1>

<%#= form_for(@task) do |f| %>

<%#= f.label :content, 'タスク' %>

<%#= f.text_field :content %>

<%#= f.submit '登録' %>

<%# end %>

<%= render 'form', task: @task %>

<% else %>

<%= redirect_to root_path %>

<% end %>
